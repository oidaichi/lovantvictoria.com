---
import { Icon } from 'astro-icon/components';

interface Props {
  content: any;
}

const { content } = Astro.props;
const { data: frontmatter } = content;
const { Content } = await content.render();

---

<div class="bg-slate-900 text-gray-300 font-sans">
  <!-- Header -->
  <header class="fixed top-0 left-0 right-0 z-50 bg-slate-900/80 backdrop-blur-sm shadow-md shadow-cyan-500/10">
    <div class="container mx-auto px-6 py-4 flex justify-between items-center">
      <a href="#" class="text-2xl font-bold text-white tracking-wider">
        <span class="text-cyan-400">LOVANT</span> VICTORIA
      </a>
      <nav class="hidden md:flex space-x-8 text-lg">
        <a href="#challenges" class="hover:text-cyan-400 transition-colors duration-300">課題解決</a>
        <a href="#strengths" class="hover:text-cyan-400 transition-colors duration-300">私たちの強み</a>
        <a href="#contact" class="hover:text-cyan-400 transition-colors duration-300">お問い合わせ</a>
      </nav>
      <button class="md:hidden text-white">
        <Icon name="mdi:menu" class="w-8 h-8" />
      </button>
    </div>
  </header>

  <!-- Hero Section -->
  <main id="home" class="pt-32 pb-20 bg-grid-slate-800">
    <section class="container mx-auto px-6 text-center">
      <h1 class="text-5xl md:text-7xl font-black text-white leading-tight mb-4">
        {frontmatter.title.split('、').map((line, index) => (
          <span class:list={["block", { 'text-cyan-400': index === 1 }]}>{line}</span>
        ))}
      </h1>
      <p class="text-xl md:text-2xl text-slate-400 max-w-4xl mx-auto mb-8">{frontmatter.subtitle}</p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="#contact" class="bg-cyan-500 hover:bg-cyan-400 text-slate-900 font-bold py-4 px-10 rounded-full transition-all duration-300 transform hover:scale-105 shadow-lg shadow-cyan-500/30">
          {frontmatter.cta_primary}
        </a>
        <a href="#" class="bg-slate-700 hover:bg-slate-600 text-white font-bold py-4 px-10 rounded-full transition-all duration-300">
          {frontmatter.cta_secondary}
        </a>
      </div>
    </section>
  </main>

  <!-- Content Section -->
  <section id="challenges" class="py-24 bg-slate-950">
    <div class="container mx-auto px-6">
      <div class="text-center mb-16">
        <h2 class="text-4xl md:text-5xl font-bold text-white mb-4">AI活用、その悩み、<span class="text-cyan-400">私たちが解決します</span></h2>
        <p class="text-lg text-slate-400 max-w-3xl mx-auto">経営存続レベルの深刻な課題から日常業務の効率化まで、AIによる戦略的解決策をご提案します。</p>
      </div>
      <div class="space-y-20">
        <Content />
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section id="contact" class="py-24 bg-slate-950">
    <div class="container mx-auto px-6 max-w-4xl text-center">
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4">今すぐ<span class="text-cyan-400">AI変革</span>を始めませんか？</h2>
      <p class="text-lg text-slate-400 mb-12">無料経営診断で、あなたの会社にとって最適なAI導入戦略をご提案します。3ヶ月で確実な成果を実感してください。</p>
      
      <div class="bg-slate-900 rounded-2xl p-8 md:p-12 border border-slate-800 shadow-lg">
        <form class="space-y-6">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <input type="text" placeholder="会社名" class="w-full px-4 py-3 bg-slate-800 border border-slate-700 rounded-lg text-white placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-cyan-500 transition-all" required />
            <input type="email" placeholder="メールアドレス" class="w-full px-4 py-3 bg-slate-800 border border-slate-700 rounded-lg text-white placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-cyan-500 transition-all" required />
          </div>
          <textarea placeholder="ご相談内容（任意）" rows="4" class="w-full px-4 py-3 bg-slate-800 border border-slate-700 rounded-lg text-white placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-cyan-500 transition-all"></textarea>
          <button type="submit" class="w-full bg-cyan-500 hover:bg-cyan-400 text-slate-900 font-bold py-4 px-8 rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg shadow-cyan-500/30">
            無料経営診断を依頼する
          </button>
        </form>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="py-12 bg-slate-900 border-t border-slate-800">
    <div class="container mx-auto px-6 text-center text-slate-500">
      <a href="#" class="text-xl font-bold text-white tracking-wider mb-4 inline-block">
        <span class="text-cyan-400">LOVANT</span> VICTORIA
      </a>
      <p class="mb-6">生成AIで、経営判断の"迷い"を"確信"に変える</p>
      <div class="flex justify-center space-x-6 mb-8">
        <a href="#" class="hover:text-cyan-400 transition-colors">プライバシーポリシー</a>
        <a href="#" class="hover:text-cyan-400 transition-colors">会社概要</a>
        <a href="#" class="hover:text-cyan-400 transition-colors">お問い合わせ</a>
      </div>
      <p>&copy; {new Date().getFullYear()} LOVANT VICTORIA. All rights reserved.</p>
    </div>
  </footer>

</div>

<style>
  .bg-grid-slate-800 {
    background-image: linear-gradient(rgba(15, 23, 42, 0.9), rgba(15, 23, 42, 0.9)),
                      linear-gradient(0deg, theme(colors.slate.800) 1px, transparent 1px),
                      linear-gradient(90deg, theme(colors.slate.800) 1px, transparent 1px);
    background-size: 100%, 40px 40px, 40px 40px;
    background-position: 0 0, 0 0, 0 0;
  }
</style>
